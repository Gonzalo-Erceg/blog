---
import { getCollection } from "astro:content";
import ArticleCards from "./ArticleCards.astro";
const blogEntries = await getCollection('blog')

---


<main class="mt-20 min-h-screen max-w-5xl mx-auto px-10" >
  <div class="text-center">
    <h2 class="text-4xl">Hola y bienvenido</h2>
    <p class="text-2xl">Mi nombre es Gonzalo y aquí mostrare mi camino por el mundo del desarrollo de videojuegos.</p>
  </div>

  <article class="mt-52">
    <h2 class="text-4xl font-semibold text-center md:text-start underline">Artículos</h2>
    <ul class="flex flex-wrap justify-center py-10">
     {blogEntries.map(entry=><ArticleCards title={entry.data.title} img={entry.data.img} published={entry.data.published} slug={entry.slug} description={entry.data.description}/>)}
    </ul>
  </article>
</main>
